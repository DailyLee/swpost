{
  "name": "swpost",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "setup": "npm install && pm2 deploy ecosystem.json production setup",
    "deploy": "npm install && pm2 deploy ecosystem.json production"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "koa": "^2.13.0",
    "koa-bodyparser": "^3.2.0",
    "koa-static": "^5.0.0",
    "pm2": "^4.5.0"
  }
}
